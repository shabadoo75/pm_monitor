---
# This playbook sets up a Pi as a PM monitor

- name: configure pi
  hosts: pm-monitor
  become_user: root
  become: yes
  tasks:
    - name: print facts
      debug:
        var: ansible_facts

  roles:
    - pi-common
